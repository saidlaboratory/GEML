# Fixed-scale efficiency, not scaling curves

No corpus-size scaling curve is produced. Any efficiency comparison must use matched fixed-scale
result rows and report parameters, FLOPs, wall time, memory, and alpha jointly. The currently
unstaged archive and absent production rows make such a comparison deferred.
