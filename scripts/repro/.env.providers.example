# Copy to an untracked, access-restricted file only when running the paid panel.
# Never commit real values. The default reproducibility mode is mocked/offline.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
MOONSHOT_API_KEY=

# Operator-supplied ceiling; the Goal 11 adapter must stop before exceeding it.
GEML_LLM_MAX_COST_USD=
